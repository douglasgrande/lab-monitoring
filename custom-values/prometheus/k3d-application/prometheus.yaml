# Habilited the component scraping kube state metrics
kubeStateMetrics:
  enabled: true

prometheus:
  prometheusSpec:
    resources:
      limits:
        cpu: 1
        memory: 1Gi
      requests:
        cpu: 250m
        memory: 512Mi
    externalLabels:
      cluster: "k3d-application"
    additionalAlertRelabelConfigs:
      - replacement: "k3d-application"
        action: replace
        target_label: "env"