prometheus:
  prometheusSpec:
    additionalAlertManagerConfigs:
      - static_configs:
          - targets: [
            "alertmanager.alertmanager.svc.cluster.local:9093"
          ]
        scheme: http
